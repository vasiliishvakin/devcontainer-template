{
  "name": "PHP DevContainer",
  "dockerComposeFile": "../compose.yml", // Point to the docker-compose.yml file you created
  "service": "php", // Specify the service name for PHP in the docker-compose.yml
  "workspaceFolder": "/app" // Match the workspace folder to the mounted volume in the docker-compose.ym
}
